{
    "issue_number": 375,
    "updated_at": "2026-05-01T12:05:51.371Z",
    "faq": [
        {
            "question": "Why do configuration settings like name prefixes revert or fail to save in the Homebridge UI?",
            "answer": "Recent updates to the Homebridge UI's underlying form rendering engine (specifically the `ng-formworks` library) introduced breaking changes in how conditional configuration schemas are processed. This can cause settings to revert to their default values immediately after being modified, result in duplicated options, or cause values to appear blank even when they are set in the configuration.\n\nTo address this behaviour:\n1. Ensure you are running `homebridge-homeconnect` version 1.7.0 or later, which implements several workarounds for these UI limitations.\n2. If you are using an older version of the plugin, you may need to downgrade the Homebridge UI to version 5.5.0, which was the last version to handle the schema correctly without workarounds.\n3. Alternatively, you can configure these settings manually by editing the `config.json` file. The structure for name prefixes is as follows:\n\n```json\n\"names\": {\n  \"prefix\": {\n    \"programs\": true,\n    \"other\": false\n  }\n}\n```\n\nIn this configuration:\n- `programs` (boolean): When `true`, appliance program names are prefixed with the appliance name.\n- `other` (boolean): When `true`, other service names (such as doors, events, or switches) are prefixed with the appliance name.",
            "semantic_abstract": "Updates to the Homebridge UI form rendering library, `ng-formworks`, caused mismatches with the plugin's conditional configuration schema, leading to UI settings reverting to defaults or appearing incorrectly. This specifically impacts the name prefixing options for appliance programs and services, requiring either plugin-level schema workarounds or manual `config.json` overrides to ensure settings persist correctly.",
            "hash": "b5c7d6533fa4028e6a77cd069448cdc4f572f77541819c0370877c8696e37263"
        }
    ]
}